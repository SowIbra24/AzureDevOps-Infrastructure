---
- name: "Mise à jour du cache APT"
  ansible.builtin.apt:
    update_cache: yes
    cache_valid_time: 3600

- name: "Installation des outils de base"
  ansible.builtin.apt:
    name:
      - curl
      - vim
      - htop
      - net-tools
    state: present